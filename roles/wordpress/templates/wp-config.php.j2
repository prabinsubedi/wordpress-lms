<?php
/**
 * WordPress Base Configuration File
 */

/** MySQL settings */
define( 'DB_NAME', '{{ db_name }}' );
define( 'DB_USER', '{{ db_user }}' );
define( 'DB_PASSWORD', '{{ db_password }}' );
define( 'DB_HOST', 'localhost' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

/**
 * Authentication Unique Keys and Salts.
 * These should be unique per installation.
 * Populate these via Ansible using lookup('password') or random values.
 */
define( 'AUTH_KEY',         '{{ auth_key }}' );
define( 'SECURE_AUTH_KEY',  '{{ secure_auth_key }}' );
define( 'LOGGED_IN_KEY',    '{{ logged_in_key }}' );
define( 'NONCE_KEY',        '{{ nonce_key }}' );
define( 'AUTH_SALT',        '{{ auth_salt }}' );
define( 'SECURE_AUTH_SALT', '{{ secure_auth_salt }}' );
define( 'LOGGED_IN_SALT',   '{{ logged_in_salt }}' );
define( 'NONCE_SALT',       '{{ nonce_salt }}' );

/** WordPress Database Table Prefix */
$table_prefix = 'wp_';

/** Enable Debugging (turn off in production) */
define( 'WP_DEBUG', false );

/** Limit post revisions (improves performance) */
define( 'WP_POST_REVISIONS', 10 );

/** Disable file editing in admin panel for security */
define( 'DISALLOW_FILE_EDIT', true );

/** Set up WordPress vars and included files */
if ( !defined('ABSPATH') ) {
    define( 'ABSPATH', '{{ install_dir }}/' );
}

require_once ABSPATH . 'wp-settings.php';
